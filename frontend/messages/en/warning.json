{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "warning": {
    "hint": {
      "document_quarantined": "Nothing was deleted — the file is at {path}. Update hestia if it came from a newer version, or delete it once you have what you need from it.",
      "export_files_embedded": "Nothing to do unless you mean to publish the pack — reinstall those files from a source first if you do.",
      "import_files_untracked": "Reinstall them from a source to make them updatable.",
      "launched_offline": "Singleplayer works as usual; launch again once you are back online to play multiplayer.",
      "modpack_files_excluded": "They would break a server. Add one under Always install in Settings to install it anyway, or turn the correction off to trust the pack.",
      "modpack_overrides_kept": "Delete a file under the game directory to take the pack's version of it at the next update.",
      "properties_schema_missing": "Any key is accepted until it can be derived again, so check spelling yourself. Updating the server re-derives it.",
      "server_list_in_use": "Close the game and make the change again, or add the server from the in-game multiplayer screen instead.",
      "session_not_verified": "Singleplayer works as usual; launch again once you are back online to play multiplayer.",
      "sync_unit_skipped": "Check the instance's file permissions, then launch it again.",
      "sync_unit_unsupported": "This instance keeps its own copy. Sharing {unit} needs Minecraft {requires} or newer."
    },
    "kind": {
      "document_quarantined": "{document} could not be read and was set aside: {detail}",
      "export_files_embedded": "{count} file(s) had no download to reference and were embedded in the archive.",
      "import_files_untracked": "{count} file(s) came from the archive itself, so they cannot be updated.",
      "launched_offline": "Playing offline as {account} — this session is not signed in.",
      "modpack_files_excluded": "{count} client-only mod(s) the pack called server-compatible were not installed.",
      "modpack_overrides_kept": "{count} file(s) you had edited were kept instead of the modpack's.",
      "properties_schema_missing": "'{name}' has no property schema, so its server.properties keys cannot be checked.",
      "server_list_in_use": "“{instance}” has {sessions} session(s) open, so the running game will overwrite this change to the multiplayer list when it exits.",
      "session_not_verified": "Could not reach Microsoft to refresh {account}, so this session runs on the stored sign-in.",
      "sync_unit_skipped": "{unit} could not be synced: {detail}",
      "sync_unit_unsupported": "“{instance}” cannot share {unit}: that needs Minecraft {requires} or newer."
    },
    "token": {
      "commands": "the command history",
      "hotbars": "the creative hotbars",
      "options": "the game settings",
      "servers": "the multiplayer list"
    }
  }
}
