#!/bin/sh
# CEF's sandbox helper must be SUID root; package builds run unprivileged, so the
# bit is set here on the installed system.
set -e
sandbox=/usr/lib/hestia/chrome-sandbox
if [ -f "$sandbox" ]; then
    chown root:root "$sandbox"
    chmod 4755 "$sandbox"
fi
exit 0
